import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  contenedor: {
    marginTop: 30,
    marginBottom: 100,
  },
  titulo: {
    color: '#64748B',
    fontSize: 30,
    textAlign: 'center',
    fontWeight: '700',
    marginTop: 20,
  },
  noGasto: {
    marginVertical: 20,
    marginTop: 20,
    textAlign: 'center',
    fontSize: 20,
  },
});

export default styles;
